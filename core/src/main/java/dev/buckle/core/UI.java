package dev.buckle.core;

/**
 * Contract for displaying UI.
 */
public interface UI {

    /**
     * Launches the UI.
     * @param configuration The config for the UI.
     */
    void launch(UIConfiguration configuration);
}
